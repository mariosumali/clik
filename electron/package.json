{
  "name": "mars-autoclicker-electron",
  "version": "0.1.0",
  "description": "Electron remake of mars-autoclicker with refreshed UI.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "lint": "node --check main.js && node --check preload.js && node --check renderer/app.js"
  },
  "devDependencies": {
    "electron": "latest"
  },
  "optionalDependencies": {
    "@jitsi/robotjs": "latest"
  }
}
